func int fib(int n) {
    if (n <= 1){
        return 1;
    }
    return fib(n-1) + fib(n-2);
}

func int main() {
    printi(fib(0));
    printi(fib(1));
    printi(fib(2));
    printi(fib(3));
    printi(fib(4));
    printi(fib(5));
    return 0;
}